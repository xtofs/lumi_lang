{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build tree binary",
            "type": "shell",
            "command": "cargo",
            "args": [
                "run",
                "--example",
                "tree"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
